import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hustlehub.app',
  appName: 'hustlehub',
  webDir: 'dist/hustlehub',
  bundledWebRuntime: false
};

export default config;
